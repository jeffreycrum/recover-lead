# Railway deployment configuration.
# Build settings are managed in the Railway dashboard (per-service Dockerfile paths).
# Only runtime / deploy behaviour is set here.

[deploy]
numReplicas = 1
sleepApplication = false
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[healthcheck]
path = "/health"
timeout = 10
